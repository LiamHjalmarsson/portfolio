import React from 'react';

const Corner = ({custom}) => {
    return (
        <div 
            className={`h-24 w-24 border-caribbean_500 ${custom} absolute`}
        >
        </div>
    );
}

export default Corner;
